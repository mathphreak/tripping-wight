Very simple container for TimeEvent's

No proper data structure, but enough for testing purpose

keep a list of TimeEvents

let people add TimeEvents, keeping things sorted by start point

let people get all TimeEvents between one time and another

let people get the length of the list
